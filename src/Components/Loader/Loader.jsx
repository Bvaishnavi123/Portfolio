import { Load } from './Styles';

const Loader = () => {
    return (
        <Load>
            <span>&#495969;</span>
            <span>&#125;</span>
        </Load>
    );
};

export default Loader;
